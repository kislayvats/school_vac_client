// Define the type for the initial state for the user slice
export interface UserSlice_ValueType {
  _id: string;
  role: string;
  email: string;
  photo: string;
  token: string;
  user_id: string;
  name: string;
  createdAt: string;
}
